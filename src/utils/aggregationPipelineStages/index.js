const chunk = require('./chunk.json');
const comment = require('./comment.json');
const file = require('./file.json');
const unwind = require('./unwind.json');

module.exports = {
    chunk,
    comment,
    file,
    unwind
};
